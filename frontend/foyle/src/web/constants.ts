export const bashLang = "bash";

// extName is the name of the extension. 
// This needs to match the value in package.json
// It is used to look up extension configuration
export const extName = "foyle";
